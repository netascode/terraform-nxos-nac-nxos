defaults:
  nxos:
    templates:
      order: 0
    devices:
      managed: true
      configuration:
        interfaces:
          loopbacks:
            shutdown: false
            ospf:
              advertise_secondaries: true
          vlans:
            shutdown: false
            ospf:
              advertise_secondaries: true
          ethernets:
            shutdown: false
            ospf:
              advertise_secondaries: true
          port_channels:
            shutdown: false
            ospf:
              advertise_secondaries: true
        routing:
          ospf_processes:
            vrfs:
              shutdown: false
          ip_prefix_lists:
            entries:
              action: permit
              criteria: exact
              from_range: 0
              to_range: 0
          route_maps:
            entries:
              additive: false
              no_community: false
              set_criteria: none
